<form method="post" action="{{ path('car_card_and_waybill_delete', {'id': car_card_and_waybill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_card_and_waybill.id) }}">
    <button class="btn">Delete</button>
</form>