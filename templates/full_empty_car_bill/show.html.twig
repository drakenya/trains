{% extends 'base.html.twig' %}

{% block title %}FullEmptyCarBill{% endblock %}

{% block body %}
    <h1>FullEmptyCarBill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ full_empty_car_bill.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('full_empty_car_bill_index') }}">back to list</a>

    <a href="{{ path('full_empty_car_bill_edit', {'id': full_empty_car_bill.id}) }}">edit</a>

    {{ include('full_empty_car_bill/_delete_form.html.twig') }}
{% endblock %}