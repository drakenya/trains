{% extends 'base.html.twig' %}

{% block title %}EmptyCarBill{% endblock %}

{% block body %}
    <h1>EmptyCarBill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empty_car_bill.id }}</td>
            </tr>
            <tr>
                <th>HomeToOrVia</th>
                <td>{{ empty_car_bill.homeToOrVia }}</td>
            </tr>
            <tr>
                <th>LoadingSpot</th>
                <td>{{ empty_car_bill.loadingSpot }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('empty_car_bill_index') }}">back to list</a>

    <a href="{{ path('empty_car_bill_edit', {'id': empty_car_bill.id}) }}">edit</a>

    {{ include('empty_car_bill/_delete_form.html.twig') }}
{% endblock %}