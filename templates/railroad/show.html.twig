{% extends 'base.html.twig' %}

{% block title %}Railroad{% endblock %}

{% block body %}
    <h1>Railroad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ railroad.id }}</td>
            </tr>
            <tr>
                <th>ReportingMark</th>
                <td>{{ railroad.reportingMark }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ railroad.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('railroad_index') }}">back to list</a>

    <a href="{{ path('railroad_edit', {'id': railroad.id}) }}">edit</a>

    {{ include('railroad/_delete_form.html.twig') }}
{% endblock %}